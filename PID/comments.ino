/*
 * PID is not suited to our motor driver, as I used a sample code which applies to another motor driver
 * PID needs Kp, Ki, Kd ajustments
 * Encoders put out the same values: @speed of 30, the outputs for both were 80 and 81
*/
 
